services:
  api:
    build: .
    env_file: .env
    command: node dist/server.js
    ports:
      - "3000:3000"
    restart: unless-stopped
  worker:
    build: .
    env_file: .env
    command: node dist/worker/index.js
    restart: unless-stopped
